# Quick Start Guide - AI Design Automation Suite

## 빠른 시작 가이드

이 가이드는 AI Design Automation Suite를 빠르게 설치하고 사용하는 방법을 설명합니다.

## 📦 설치 (5분)

### 1단계: Python 확인
```bash
python --version
```
Python 3.8 이상이 필요합니다.

### 2단계: 저장소 다운로드
```bash
git clone https://github.com/junggyeol4444/auto.git
cd auto
```

또는 ZIP 파일 다운로드 후 압축 해제

### 3단계: 의존성 설치
```bash
pip install Pillow opencv-python numpy customtkinter
```

**선택사항** (고급 기능):
```bash
pip install rembg scikit-image scipy
```

## 🚀 실행

```bash
python main.py
```

## 📖 사용 방법

### 유튜브 썸네일 만들기 (3분)

1. **"유튜브 썸네일"** 탭 클릭

2. **제목 입력**
   ```
   예: "완전 초간단 꿀팁 대공개!"
   ```

3. **강조 단어 입력** (선택)
   ```
   예: 초간단, 꿀팁, 대공개
   ```

4. **이미지 업로드**
   - "이미지 업로드" 버튼 클릭
   - 사진 선택 (JPG, PNG)

5. **장르 선택**
   - 게임, 브이로그, 리뷰, 먹방, 교육 중 선택

6. **색상 조합 선택**
   - 빨강+노랑, 파랑+흰색, 검정+금색, 형광 중 선택

7. **생성 시작** 클릭
   - 9가지 버전이 자동 생성됨
   - ◀ ▶ 버튼으로 버전 선택

8. **저장**
   - "저장" 또는 "모두 저장" 클릭
   - `output/thumbnails/` 폴더에 저장됨

### 배경 제거하기 (1분)

1. **"사진 합성"** 탭 클릭

2. **기능 선택**: "배경 제거"

3. **이미지 업로드**
   - "이미지 업로드" 버튼 클릭
   - 배경을 제거할 사진 선택

4. **처리 시작** 클릭
   - 자동으로 배경 제거됨
   - 오른쪽에 결과 미리보기

5. **저장** 클릭
   - `output/synthesis/` 폴더에 저장됨

### 배경 바꾸기 (2분)

1. **"사진 합성"** 탭에서 **기능 선택**: "배경 교체"

2. **옵션 설정**
   - 배경 종류: 단색 또는 그라데이션
   - 배경 색상: 흰색, 검정, 파랑, 빨강 등

3. **이미지 업로드** → **처리 시작** → **저장**

### 콜라주 만들기 (2분)

1. **기능 선택**: "콜라주"

2. **그리드 크기 선택**
   - 2x2, 3x3, 2x3, 3x2

3. **여러 이미지 업로드**
   - "여러 이미지 업로드" 버튼 클릭
   - 여러 사진 선택 (Ctrl/Cmd + 클릭)

4. **간격 설정** (선택)
   - 기본값: 10 픽셀

5. **처리 시작** → **저장**

### 색감 통일하기 (2분)

1. **기능 선택**: "색감 통일"

2. **필터 선택**
   - original, vintage, vivid, warm, cool

3. **여러 이미지 업로드**
   - 색감을 통일할 여러 사진 선택

4. **처리 시작** → **저장**

## 💡 팁과 요령

### 썸네일 제작
- **짧고 강렬한 제목** 사용 (15자 이내)
- **핵심 키워드 2-3개** 강조
- **밝고 명확한 이미지** 선택
- **여러 버전 생성**하여 A/B 테스트

### 사진 합성
- **고해상도 이미지** 사용 권장
- **배경 제거**는 **흰색/단색 배경**에서 가장 잘 작동
- **콜라주**는 **비슷한 비율의 이미지** 사용 권장

### 성능 최적화
- 이미지 크기가 클수록 처리 시간 증가
- 필요한 경우 미리 리사이즈
- 첫 실행은 모델 다운로드로 시간이 걸릴 수 있음

## ❓ 자주 묻는 질문

### Q: 한글이 깨져요
**A**: 시스템에 한글 폰트를 설치하세요
- Windows: 맑은 고딕 (기본 설치됨)
- macOS: Apple SD Gothic Neo (기본 설치됨)
- Linux: `sudo apt install fonts-nanum`

### Q: rembg 설치가 안 돼요
**A**: 선택사항입니다. 설치하지 않아도 기본 배경 제거 기능을 사용할 수 있습니다.

### Q: 처리가 너무 느려요
**A**: 
1. 이미지 크기를 줄이세요
2. 불필요한 기능 비활성화
3. GPU가 있다면 torch를 GPU 버전으로 설치

### Q: 생성된 파일은 어디 있나요?
**A**: 
- 썸네일: `output/thumbnails/`
- 사진 합성: `output/synthesis/`

### Q: 여러 개를 한번에 처리할 수 있나요?
**A**: 콜라주와 색감 통일 기능에서 여러 이미지를 동시에 처리할 수 있습니다.

### Q: 상업적으로 사용해도 되나요?
**A**: 네, 자유롭게 사용 가능합니다. 단, 생성된 이미지의 저작권은 사용자에게 있습니다.

## 🐛 문제 발생 시

### 오류 메시지가 나와요
1. Python 버전 확인 (3.8+)
2. 의존성 재설치: `pip install -r requirements.txt`
3. GitHub Issues에 오류 메시지와 함께 문의

### 프로그램이 실행되지 않아요
```bash
# 진단 스크립트 실행
python test_basic.py
```

### 기능이 작동하지 않아요
1. 이미지 형식 확인 (JPG, PNG 권장)
2. 이미지 크기 확인 (너무 크지 않은지)
3. 에러 메시지 확인

## 📚 더 알아보기

- **상세 문서**: `README.md`
- **아키텍처**: `ARCHITECTURE.md`
- **기여 가이드**: `CONTRIBUTING.md`
- **예제 코드**: `example_usage.py`

## 🎉 이제 시작하세요!

```bash
python main.py
```

즐거운 디자인 되세요! 🎨
